# Generated — never commit
unityLibrary/
libs/
shared/
build/
**/build/
.gradle/
.kotlin/
.idea/
local.properties
*.log
*.apk

# Contract generated
node_modules/
dist/
contract/src/managed/
contract/midnight-level-db/
# Wallet proving keys (#256) — downloaded + staged by provisionWalletKeys when
# bundleWalletKeys is on. ~33MB of regenerable bytes — sourced, not stored
# (unlike the small contract keys under assets/keys/, which ARE tracked here
# because the contract source above is gitignored).
app/src/main/assets/wallet-keys/

# Unity generated (only Assets/, Packages/, ProjectSettings/ are source)
unity/Library/
unity/Temp/
unity/Logs/
unity/UserSettings/
unity/build/
unity/*.csproj
unity/*.slnx
unity/*.apk
unity/*_BackUpThisFolder_ButDontShipItWithYourGame/
unity/*_BurstDebugInformation_DoNotShip/

# Unity AI Assistant scratch cache (regenerates every use, noisy diffs)
unity/Assets/AI Toolkit/Temp/
unity/Assets/AI Toolkit/Temp.meta

# Unity Editor session-recovery snapshots (auto-created after an
# interrupted save / crash; not part of the project).
unity/Assets/_Recovery/
unity/Assets/_Recovery.meta

# OS
.DS_Store
